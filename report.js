let last=null;document.addEventListener('DOMContentLoaded',()=>{document.getElementById('load').onclick=load;document.getElementById('export').onclick=exportCsv;load()});async function load(){const box=document.getElementById('report');box.innerHTML='Đang tải...';last=await VQTH6.getReports();box.innerHTML=`<div class="grid grid-4"><div class="card"><b>${last.missingVideo?.length||0}</b><br>Chưa xem video</div><div class="card"><b>${last.missingPractice?.length||0}</b><br>Chưa luyện tập</div><div class="card"><b>${last.below70?.length||0}</b><br>Dưới 70%</div><div class="card"><b>${last.stuckStudents?.length||0}</b><br>Cần hỗ trợ</div></div>`+table('🔴 Học sinh cần hỗ trợ',last.stuckStudents)+table('⚠️ Kiểm tra dưới 70%',last.below70)+table('🎬 Chưa xem video',last.missingVideo)+table('📝 Chưa luyện tập',last.missingPractice)}function table(title,rows=[]){return `<h2>${title}</h2><div class="table-wrap"><table class="table"><thead><tr><th>Mã HS</th><th>Trạm</th><th>Điểm</th><th>Lần</th><th>Trạng thái</th></tr></thead><tbody>${rows.length?rows.map(r=>`<tr><td>${r.maHS||''}</td><td>${r.title||''}</td><td>${r.score??''}</td><td>${r.attempt??''}</td><td>${r.status||''}</td></tr>`).join(''):'<tr><td colspan="5">Không có dữ liệu</td></tr>'}</tbody></table></div>`}function exportCsv(){if(!last)return;const rows=[];['missingVideo','missingPractice','below70','stuckStudents'].forEach(k=>(last[k]||[]).forEach(x=>rows.push({type:k,...x})));const cols=[...new Set(rows.flatMap(r=>Object.keys(r)))];const csv=cols.join(',')+'\n'+rows.map(r=>cols.map(c=>'"'+String(r[c]??'').replaceAll('"','""')+'"').join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='bao-cao-vqth6-v51.csv';a.click()}
